/**
 * Seed mock Redis with test data (optional)
 * Add your seed data here for local development
 */
export async function seedMockRedis(): Promise<void> {
  console.log('[Server] Mock Redis ready (no seed data)');

  // Example: seed test data
  // import { redis } from '../devvitProxy';
  // await redis.set('test-key', 'test-value');
}
